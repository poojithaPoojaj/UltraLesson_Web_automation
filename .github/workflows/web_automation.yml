name: Ultralesson Web Automation

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'adopt'
    - name: Build with Gradle
      uses: gradle/gradle-build-action@4137be6a8bf7d7133955359dbd952c0ca73b1021
      with:
        arguments: build

  #web_test:
   # runs-on: ubuntu-latest
   # strategy:
    #  matrix:
     #   browser: [chrome, firefox, safari]
    #needs: build
    #steps:
     # - uses: actions/checkout@v2
      #- name: Set up JDK 11
       # uses: actions/setup-java@v2
        #with:
        #  java-version: '11'
         # distribution: 'adopt'
          #cache: gradle

      #- name: Run Web Tests in ${{ matrix.browser }}
        #uses: gradle/gradle-build-action@4137be6a8bf7d7133955359dbd952c0ca73b1021
       # with:
        #  arguments: runWebTests -Dbrowser.name=${{ matrix.browser }} -Dbrowser.headless=true
  
  responsive_view:
    needs: build
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
    
    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: 16
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run mobile enumeration
      run: |
        # Run your mobile enumeration command here
        # For example:
        # npx mobile-enumeration -o results.txt
        echo "Running mobile enumeration..."
        # Replace the above command with the actual command to run your mobile enumeration tool.
    
    - name: Upload results
      run: |
        # Upload the results to a cloud storage or an artifact
        # For example, using GitHub Actions' artifacts:
        echo '::set-env name=MOBILE_ENUM_RESULTS::results.txt'
        echo 'Results from mobile enumeration:'
        cat results.txt >> ${{ env.MOBILE_ENUM_RESULTS }}
        echo '::set-output name=mobile_enum_results::${{ env.MOBILE_ENUM_RESULTS }}'
      # Note: `results.txt` is the file containing the results of the mobile enumeration. Modify this step to suit your actual use case.
    
    - name: Cleanup
      run: |
        # Cleanup any generated files or artifacts
        rm results.txt

